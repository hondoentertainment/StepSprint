services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: stepsprint
    ports:
      - "5432:5432"
    volumes:
      - stepsprint_pgdata:/var/lib/postgresql/data

volumes:
  stepsprint_pgdata:
